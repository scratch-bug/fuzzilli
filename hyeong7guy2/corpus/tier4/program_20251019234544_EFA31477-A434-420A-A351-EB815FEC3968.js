Symbol.iterator;
("🙌🏿")[1];
function F4(a6) {
    if (!new.target) { throw 'must be called with new'; }
    class C7 {
    }
    let v8 = 16;
    v8++;
    parseInt(C7, v8);
}
new F4();
for (let v15 = 0; v15 < 500; v15++) {
    v15 < 20000;
    ("15").normalize();
    v15++;
}
