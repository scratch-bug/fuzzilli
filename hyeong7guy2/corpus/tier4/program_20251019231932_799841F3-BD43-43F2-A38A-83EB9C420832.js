function F0() {
    if (!new.target) { throw 'must be called with new'; }
    for (let v3 = 0; v3 < 5; v3++) {
        function f4(a5, a6) {
            return `keys${`sticky${-1073741824}supportedValuesOf${ArrayBuffer}e`}V`;
        }
        f4(F0, F0);
        %OptimizeFunctionOnNextCall(f4);
    }
}
new F0();
